{
  "name": "sub-wave",
  "version": "1.13.0",
  "private": true,
  "description": "SUB/WAVE — personal internet radio station. HLS + Icecast, AI DJ, AI-curated playlists.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "up": "docker compose up -d",
    "down": "docker compose down",
    "pull": "docker compose pull",
    "logs": "docker compose logs -f",
    "lint": "npm --prefix controller run lint && npm --prefix web run lint",
    "test": "npm --prefix controller test",
    "gen:schemas": "npm --prefix controller run gen:schemas"
  }
}
